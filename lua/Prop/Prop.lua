local PropNumber = require('Prop.PropNumber')
local PropSwitch = require('Prop.PropSwitch')
local PropPercent = require('Prop.PropPercent')
local PropList = require('Prop.PropList')

Prop = {
  Number = PropNumber,
  Switch = PropSwitch,
  Percent = PropPercent,
  List = PropList,
}
